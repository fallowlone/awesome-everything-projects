# Палитра команд

Палитра команд ⌘K с нечётким ранжированием, асинхронными источниками действий и полным клавиатурным управлением (стрелки, Enter, Escape, области видимости) — интерактивный слой, который нужен каждому профессиональному инструменту.

**Сложность:** intermediate · **Оценка, дней:** 3 · **Стек:** preact, typescript · **Треки:** frontend

## Что сдаём

Палитра, открывающаяся по ⌘K, нечётко фильтрующая зарегистрированные команды при вводе, поддерживающая вложенные области (например, вход в подменю 'Theme >') и полностью управляемая только с клавиатуры.

## Зачем этот проект

Палитры команд обманчиво малы, но затрагивают удивительно широкий спектр сложных задач: нечёткое ранжирование, ощущающееся правильным, асинхронные источники без гонок, вложенное навигационное состояние и доступное клавиатурное управление. Собери её один раз правильно — и больше никогда не потянешься за библиотекой, заодно поняв, почему каждая библиотека что-то из этого делает неправильно.

## Навыки

- fuzzy search / scoring (fzf-style)
- async data sources with debounce
- focus trap + ARIA combobox
- command registry pattern

## Этапы

### 1. Реестр + управление с клавиатуры

Собери реестр команд с синхронными командами, открытием/закрытием через ⌘K и обработкой клавиш стрелок, Enter и Escape.

**Критерии готовности:**

- ⌘K открывает/закрывает палитру; стрелки двигают активный пункт, Enter запускает, Escape закрывает — всё без мыши.
- Фокус заперт внутри открытой палитры и возвращается на триггер при закрытии.

### 2. Нечёткое ранжирование

Добавь нечёткую фильтрацию со scoring-функцией, ставящей точные префиксные совпадения выше рассеянных, с подсветкой совпавших символов.

**Критерии готовности:**

- При вводе точные префиксные совпадения ранжируются выше разбросанных подпоследовательностей, а совпавшие символы подсвечиваются.
- Пустой запрос показывает все команды; нет совпадений — явное пустое состояние.

### 3. Асинхронные источники с дебаунсом

Поддержи асинхронные источники действий (например, search API) с дебаунсом, индикатором загрузки и корректной отменой устаревших запросов.

**Критерии готовности:**

- Асинхронный источник дебаунсится, показывает загрузку, а устаревший in-flight запрос отменяется при смене ввода.
- Результаты отменённого запроса никогда не перезаписывают результаты последнего запроса.

## Рубрика

### Качество совпадения

- **Джуниор:** Фильтрует элементы по вхождению подстроки; все совпадающие элементы выводятся без учёта позиции.
- **Мидл:** Реализует поиск по подпоследовательности (не подстроке) и ставит префиксные / смежные совпадения выше рассеянных, чтобы нужный элемент оказывался первым.
- **Синьор:** Функция оценки чистая и тестируется отдельно; ничьи разрешаются детерминированно; функция корректно обрабатывает границы Unicode, а её временная сложность указана и обоснована (O(n·m) на элемент, итого O(N·m log N)).

### Корректность клавиатурной модели

- **Джуниор:** Стрелки перемещают выделение, Enter запускает выделенный элемент; перенос отсутствует или ломается на границах.
- **Мидл:** Выделение правильно оборачивается в обоих направлениях, включая список из одного элемента или пустой; reduce — чистая функция без побочных эффектов.
- **Синьор:** Редьюсер — единственный источник истины для всего клавиатурного состояния; управление фокусом (ловушка при открытии, восстановление при закрытии) вынесено за пределы редьюсера, чтобы чистая логика тестировалась в Node без DOM; граничные случаи (пустой список, быстрые нажатия) покрыты юнит-тестами.

### Производительность под нагрузкой

- **Джуниор:** Перестраивает весь список при каждом нажатии клавиши синхронно; незаметно при коротких списках (< 50 элементов).
- **Мидл:** Асинхронные источники дебаунсятся; функция ранжирования мемоизируется или пропускается при неизменном запросе; устаревшие in-flight запросы отменяются.
- **Синьор:** Список из 10 000+ команд отображается без лагов: либо виртуализирован (в DOM только видимые строки), либо проиндексирован заранее (trie / инвертированный индекс строится один раз при регистрации, чтобы работа на каждое нажатие была сублинейной). Выбор обоснован измеренным компромиссом между памятью и CPU.

## Задачи со звёздочкой

- Добавь области видимости: команда может открывать новый контекст (например, 'Switch project >') со своим отфильтрованным списком, навигацией-хлебными крошками и клавишей Backspace для выхода из области.
- Сделай палитру доступной: правильные ARIA-роли combobox + listbox, объявления live region о количестве результатов и focus trap, восстанавливающий фокус при закрытии.

---

Полная версия проекта, прогресс и связанные уроки: https://fallowlone.com/ru/projects/command-palette
